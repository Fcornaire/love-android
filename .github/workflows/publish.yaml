name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v12.0)"
        required: true
      ref:
        description: "Branch or commit to build (default: main)"
        required: false
        default: "main"
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GRADLE_OPTS: "-Dorg.gradle.jvmargs='-Xmx4G'"
    steps:
      - name: Prepare Environment
        run: sudo apt-get update && curl -Lo kitware-archive.sh https://apt.kitware.com/kitware-archive.sh && sudo bash ./kitware-archive.sh
      - name: Install Dependencies
        run: sudo apt-get install ninja-build cmake
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}
          submodules: true
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: adopt-hotspot
          java-version: 17
          cache: gradle
      - name: Build Embed APKs
        run: bash ./gradlew assembleEmbedRecordRelease assembleEmbedNoRecordRelease
      - name: Build Embed AABs
        run: bash ./gradlew bundleEmbedRecordRelease bundleEmbedNoRecordRelease

      - name: Artifact (Embed APK)
        uses: actions/upload-artifact@v4
        with:
          name: love-android-embed.apk
          path: app/build/outputs/apk/embedRecord/release/app-embed-record-release-unsigned.apk
      - name: Artifact (Embed APK w/o recording)
        uses: actions/upload-artifact@v4
        with:
          name: love-android-embed-norecording.apk
          path: app/build/outputs/apk/embedNoRecord/release/app-embed-noRecord-release-unsigned.apk
      - name: Artifact (Embed AAB)
        uses: actions/upload-artifact@v4
        with:
          name: love-android-embed.aab
          path: app/build/outputs/bundle/embedRecordRelease/app-embed-record-release.aab
      - name: Artifact (Embed AAB w/o recording)
        uses: actions/upload-artifact@v4
        with:
          name: love-android-embed-norecording.aab
          path: app/build/outputs/bundle/embedNoRecordRelease/app-embed-noRecord-release.aab
      - name: Artifact (Debug symbols)
        uses: actions/upload-artifact@v4
        with:
          name: love-android-unstripped-debugsyms-Release
          path: app/build/intermediates/cxx

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: Release ${{ github.event.inputs.tag }}
          draft: false
          prerelease: ${{ github.event.inputs.prerelease }}
          files: |
            app/build/outputs/apk/embedRecord/release/app-embed-record-release-unsigned.apk
            app/build/outputs/apk/embedNoRecord/release/app-embed-noRecord-release-unsigned.apk
            app/build/outputs/bundle/embedRecordRelease/app-embed-record-release.aab
            app/build/outputs/bundle/embedNoRecordRelease/app-embed-noRecord-release.aab
